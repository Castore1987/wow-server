---
description: Crea o modifica un módulo de AzerothCore
argument-hint: [qué tiene que hacer el módulo]
---
Módulo de AzerothCore: **$ARGUMENTS**

Seguí la skill `modulos-y-cpp`. Antes de escribir C++, justificá en una línea
por qué esto no se resuelve con SmartAI ni con config. Entregá el módulo
completo (CMakeLists, loader, src, conf con `Enable = 0` por defecto, SQL
idempotente, README), los pasos de compilación y cómo probarlo in-game.
Marcá `[VERIFICAR]` cada firma de hook contra el `ScriptMgr.h` de esta versión.
