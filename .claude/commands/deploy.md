---
description: Prepara y ejecuta un despliegue con backup previo
argument-hint: [qué se despliega]
---
Deploy: **$ARGUMENTS**

Seguí la skill `operacion-servidor`. Recorré los 10 pasos del checklist de
deploy en orden, sin saltear el backup ni la re-aplicación de
`sql/custom/overrides/`. Antes de empezar, decime cuál es el plan de rollback
concreto para este deploy y en cuánto tiempo se ejecuta.
